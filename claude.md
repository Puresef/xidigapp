your job is to build the perfect award-winning app. always push back if anything comes up that could hinder that.

Design target is always the full-quality experience. Never cut, hide, or
simplify a feature for bandwidth — defer bytes via MediaSlot. Every interactive
element ships hover/pressed/focus-visible/disabled; every list ships
skeleton/empty/error. Verify AA contrast ≥4.5:1 on the dark palette. If a build
instruction ever conflicts with this bar, flag it instead of building low.

See DESIGN.md (design target, tokens, states quality bar) and docs/lite-mode.md (the normative MediaSlot/Lite contract).
