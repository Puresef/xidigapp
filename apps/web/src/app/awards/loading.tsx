import { LoadingShell } from '@/components/route-loading';

/** /awards instant loading state (Task 9): title + page-level comet. */
export default function AwardsLoading() {
  return <LoadingShell titleKey="awards.title" />;
}
