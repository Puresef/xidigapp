# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
build/
*.tsbuildinfo

# Test / coverage
coverage/

# Env — never commit real secrets. .env.example IS committed.
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Note: next-env.d.ts is intentionally NOT ignored so a fresh clone typechecks
# before the first `next build`.

# Supabase CLI transient state (linked project ref, pooler cache)
packages/db/supabase/.temp/

# Old-site salvage crate (temporary; deploy via xidig/DEPLOY-TOURNIQUET.md, delete after port)
/xidig/
.superpowers/
